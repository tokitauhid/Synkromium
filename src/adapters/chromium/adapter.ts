/**
 * adapter.ts — The Chromium browser translator.
 *
 * This is the heart of Synkromium's first adapter. It knows how
 * to read Chrome/Chromium settings files, clean them up into
 * a safe format, and write them back without breaking anything.
 *
 * It follows the rules defined in base.ts — extract, restore, validate.
 *
 * Implementation comes in Step 7.
 */
