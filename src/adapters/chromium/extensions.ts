/**
 * extensions.ts — The extension list manager.
 *
 * This module reads the list of browser extensions you have
 * installed and saves just the IDs and names — not the actual
 * extension files (those are huge and downloadable anyway).
 *
 * On another device, it can use these IDs to tell Chrome
 * "hey, install these extensions" so your browser matches.
 *
 * Implementation comes in Step 8.
 */
